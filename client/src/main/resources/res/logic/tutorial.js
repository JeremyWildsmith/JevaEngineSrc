var hasBeenOver = false;

function onTrigger(character, isOver)
{
/*	if(!hasBeenOver && character == game.getPlayer())
	{
		hasBeenOver = true;
		me.dialog(character, 0);
	}*/
}

function onDialogEvent(character, event)
{
	return -1;
}